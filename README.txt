Instalar todas las dependencias con npm install

ejecutar npm start (para usar con pm2 ejecutar lo siguiente pm2 start npm -- start )

Para ver el listado de registrados acceder a http://localhost:2400/listProfiles
para crear un usuario acceder a http://localhost:2400/formProfile
